

const LoadAllcraftItem = () => {

    // const { _id, image, item_name, subcategory_Name, short_description, price, rating, customization, processing_time, stockStatus, User_Email, User_Name } = artCraft;
    // console.log(artCraft)
    return (
      <div>

      </div>
    );
};

export default LoadAllcraftItem;